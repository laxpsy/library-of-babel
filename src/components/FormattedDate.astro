---
interface Props {
	date: Date;
	blog: boolean;
}

const { date, blog = false } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-in',{
			year: 'numeric',
			month: `${blog? 'long' : '2-digit'}`,
			day: '2-digit'
		})
	}
</time>
