---
const { title, date, slug } = Astro.props as {
  title: string;
  date: string;
  slug: string;
};
---

<div class="flex flex-row gap-x-1">
  <p class="text-directory-prefix">
    {date}
  </p>
  <p class="text-neutral-400">|</p>
  <a href={slug}>
    {title}
  </a>
</div>
